var expect = require('chai').expect;

describe("When this test is run", function () {
    it("Should aways pass with true", function () {
        expect(true).to.equal(true);
    });

    it("should always pass with false", function () {
        expect(false).to.equal(false);
    });
});
